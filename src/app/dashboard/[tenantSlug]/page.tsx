import DashboardPage from "../page";

// El selector de negocio llega aquí cuando se cambia desde una ruta global,
// como Plataforma. Así cada negocio abre su propio inicio operativo.
export default DashboardPage;
